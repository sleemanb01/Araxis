node_modules/
.expo/
dist/
.env
.env.local
*.log
.DS_Store

# Firebase native config (inject via EAS secrets / file env vars)
google-services.json
GoogleService-Info.plist

# App Store Connect API key — secret, never commit
*.p8

# Google Play service account key — secret, never commit
google-play-service-account.json
*play-service-account*.json

# App Store assets (large binaries — keep out of git)
screenshots/

rules_version = '2';

// Araxis v2 does not use Firebase Storage (no uploads). Deny everything.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
